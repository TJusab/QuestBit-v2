export enum UserIcon {
    Woman = "Woman",
    Man = "Man",
    Default = "Default"
}

export enum QuestIcon {
    Chest = "Chest",
    Default = "Default"
}

export enum Status {
    Unassigned = "Unassigned",
    Assigned = "Assigned",
    OnGoing = "On Going",
    Completed = "Completed"
}